window.products = <%= @products.to_json(include: :images).html_safe %>;
Content.page = <%= @page %>;
UI.display('<%=j render partial: search_result(@products), layout: 'products/grid', locals: { grid_size: 'four wide' } %>');
Products.init();