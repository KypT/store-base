<%= render partial: '/sub_header', locals: {title: 'Отзывы' } %>

<section class="reviews-container">
  <div class="reviews clear">
    <%= render partial: @reviews %>
  </div>
  <%= render partial: 'reviews/new' %>
</section>