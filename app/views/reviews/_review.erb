<article class="review clear" data-id="<%= review.id %>">
  <div class="img-container">
    <%= image_tag image_for(review), class: 'cover' %>
  </div>
  <h2 class="author"><%= review.author %></h2>
  <p class="content"><%= review.review %></p>
</article>