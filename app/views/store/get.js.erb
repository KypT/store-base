Products.set( <%= @products.to_json(include: [:images, :tags, :category]).html_safe %> );
Content.page = <%= @page %>;
Store.displayContent('<%=j render partial: 'products/grid', locals: { grid_size: 'four wide', products: @products } %>');
